{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default permissions for FlowCash desktop engine",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:read-files",
    "fs:write-files"
  ]
}
